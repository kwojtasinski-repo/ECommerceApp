@model UserProfileListVm
@{
    ViewData["Title"] = "Moje profile";
    var totalPages = Model.PageSize > 0 ? (int)Math.Ceiling((double)Model.Count / Model.PageSize) : 1;
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h1 class="h3">Moje profile</h1>
    <a asp-action="Create" class="btn btn-primary">Nowy profil</a>
</div>

<form method="post">
    <div class="row mb-3 g-2">
        <div class="col-auto">
            <select name="pageSize" class="form-select">
                <option value="10" selected="@(Model.PageSize == 10)">10</option>
                <option value="25" selected="@(Model.PageSize == 25)">25</option>
                <option value="50" selected="@(Model.PageSize == 50)">50</option>
            </select>
        </div>
        <div class="col">
            <input name="searchString" value="@Model.SearchString" class="form-control" placeholder="Szukaj..." />
        </div>
        <div class="col-auto">
            <button type="submit" class="btn btn-secondary">Szukaj</button>
        </div>
    </div>
</form>

@if (Model.Profiles.Count == 0)
{
    <p class="text-muted">Brak profili. <a asp-action="Create">Utwórz profil</a>.</p>
}
else
{
    <table class="table table-hover">
        <thead>
            <tr>
                <th>Imię i nazwisko</th>
                <th>Firma</th>
                <th>Email</th>
                <th>Akcje</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var p in Model.Profiles)
            {
                <tr>
                    <td>@p.FirstName @p.LastName</td>
                    <td>@(p.IsCompany ? p.CompanyName ?? "—" : "—")</td>
                    <td>@p.Email</td>
                    <td>
                        <a asp-action="Details" asp-route-id="@p.Id" class="btn btn-sm btn-outline-primary">Szczegóły</a>
                        <a asp-action="Edit" asp-route-id="@p.Id" class="btn btn-sm btn-outline-secondary">Edytuj</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    @if (totalPages > 1)
    {
        <nav>
            <ul class="pagination">
                @for (int i = 1; i <= totalPages; i++)
                {
                    <li class="page-item @(i == Model.CurrentPage ? "active" : "")">
                        <form method="post" class="d-inline">
                            <input type="hidden" name="pageSize" value="@Model.PageSize" />
                            <input type="hidden" name="pageNo" value="@i" />
                            <input type="hidden" name="searchString" value="@Model.SearchString" />
                            <button type="submit" class="page-link">@i</button>
                        </form>
                    </li>
                }
            </ul>
        </nav>
    }
}
