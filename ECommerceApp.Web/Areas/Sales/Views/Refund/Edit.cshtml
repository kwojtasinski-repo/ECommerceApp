@model RefundDetailsVm

@{
    ViewData["Title"] = "Obsługa zwrotu";
}

<h1>Obsługa zwrotu</h1>

<dl class="row">
    <dt class="col-sm-3">Id</dt>
    <dd class="col-sm-9">@Model.Id</dd>

    <dt class="col-sm-3">Id zamówienia</dt>
    <dd class="col-sm-9">
        <a asp-area="Sales" asp-controller="Orders" asp-action="Details" asp-route-id="@Model.OrderId">@Model.OrderId</a>
    </dd>

    <dt class="col-sm-3">Powód</dt>
    <dd class="col-sm-9">@Model.Reason</dd>

    <dt class="col-sm-3">Gwarancja</dt>
    <dd class="col-sm-9">@(Model.OnWarranty ? "Tak" : "Nie")</dd>

    <dt class="col-sm-3">Status</dt>
    <dd class="col-sm-9">@Model.Status</dd>

    <dt class="col-sm-3">Data zgłoszenia</dt>
    <dd class="col-sm-9">@Model.RequestedAt.ToString("dd.MM.yyyy HH:mm")</dd>

    @if (Model.ProcessedAt.HasValue)
    {
        <dt class="col-sm-3">Data rozpatrzenia</dt>
        <dd class="col-sm-9">@Model.ProcessedAt.Value.ToString("dd.MM.yyyy HH:mm")</dd>
    }
</dl>

@if (Model.Items != null && Model.Items.Count > 0)
{
    <h4>Pozycje zwrotu</h4>
    <table class="table">
        <thead>
            <tr>
                <th>Id produktu</th>
                <th>Ilość</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Items)
            {
                <tr>
                    <td>@item.ProductId</td>
                    <td>@item.Quantity</td>
                </tr>
            }
        </tbody>
    </table>
}

@if (Model.Status == nameof(RefundStatus.Requested))
{
    <div class="d-flex gap-2 mt-3">
        <form asp-area="Sales" asp-controller="Refund" asp-action="Approve" method="post">
            <input type="hidden" name="id" value="@Model.Id" />
            <button type="submit" class="btn btn-success">Zatwierdź zwrot</button>
        </form>

        <form asp-area="Sales" asp-controller="Refund" asp-action="Reject" method="post">
            <input type="hidden" name="id" value="@Model.Id" />
            <button type="submit" class="btn btn-danger">Odrzuć zwrot</button>
        </form>

        <a asp-area="Sales" asp-controller="Refund" asp-action="Index" class="btn btn-secondary">Wróć</a>
    </div>
}
else
{
    <div class="mt-3">
        <a asp-area="Sales" asp-controller="Refund" asp-action="Index" class="btn btn-secondary">Wróć</a>
    </div>
}
