@model ECommerceApp.Application.Backoffice.ViewModels.BackofficeRefundDetailVm

@{
    ViewData["Title"] = "Szczegóły zwrotu";
}

<h1>Szczegóły zwrotu</h1>

<dl class="row">
    <dt class="col-sm-3">Id</dt>
    <dd class="col-sm-9">@Model.Id</dd>

    <dt class="col-sm-3">Id zamówienia</dt>
    <dd class="col-sm-9">@Model.OrderId</dd>

    <dt class="col-sm-3">Id klienta</dt>
    <dd class="col-sm-9">@Model.CustomerId</dd>

    <dt class="col-sm-3">Powód</dt>
    <dd class="col-sm-9">@Model.Reason</dd>

    <dt class="col-sm-3">Status</dt>
    <dd class="col-sm-9">@Model.Status</dd>

    <dt class="col-sm-3">Gwarancja</dt>
    <dd class="col-sm-9">@(Model.OnWarranty ? "Tak" : "Nie")</dd>
</dl>

<a asp-area="Backoffice" asp-controller="Refunds" asp-action="Index">← Wróć do listy</a>
