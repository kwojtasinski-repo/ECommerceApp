@model System.Collections.Generic.List<CategoryVm>
@{
    ViewData["Title"] = "Kategorie";
}

<div class="container-fluid mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2><i class="fa-solid fa-list me-2"></i>Kategorie</h2>
        <a asp-action="Create" class="btn btn-primary"><i class="fa-solid fa-plus me-1"></i>Dodaj kategorię</a>
    </div>

    <div class="table-responsive">
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>Nazwa</th>
                    <th>Slug</th>
                    <th>Akcje</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var cat in Model)
                {
                    <tr>
                        <td>@cat.Name</td>
                        <td>@cat.Slug</td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@cat.Id" class="btn btn-outline-warning btn-sm">Edytuj</a>
                            <form asp-action="Delete" asp-route-id="@cat.Id" method="post" class="d-inline">
                                @Html.AntiForgeryToken()
                                <button type="submit" class="btn btn-outline-danger btn-sm"
                                        onclick="return confirm('Czy na pewno usunąć kategorię?')">Usuń</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
